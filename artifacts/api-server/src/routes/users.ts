// Legacy Clerk user-sync routes replaced by JWT auth.
// User management is now in /app-users.
// This stub is kept so existing imports in app.ts resolve without error.

import { Router } from "express";

const router = Router();

export default router;
